tree drawn on the note book


object z created will fall into a loop since its end pointer which is null when make-cycle is introduced it will point to the starting point of the same object hence a never ending loop 
