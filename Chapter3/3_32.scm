In the simulator given output of one gate or logical device is always an input to another and it all depends upon the delay each gates takes in order to evaluate. Therefore it needs to be first in first out 

There is one more thing because of which we are using the FIFO sample because we are already sorting the queue with repect to the time so because of that we need to exeute the first in first
 

 