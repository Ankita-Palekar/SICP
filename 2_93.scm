We need to incorporate polynomial package with the rational package that is in place of simple add mul divide we will have polynimials 


(define (install-rational-package)
	(define (numer x) (car x))
	(define (denom x)(cdr x))
)